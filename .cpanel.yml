---
deployment:
  tasks:
    - export DEPLOYPATH=/home/pivotalvoice/Pivotal-Voice
    - /bin/cp -R * $DEPLOYPATH/
    - /bin/cp -R .env.local.example $DEPLOYPATH/ 2>/dev/null; true
    - . /home/pivotalvoice/nodevenv/pivotal-voice/24/bin/activate && cd $DEPLOYPATH && npm install
